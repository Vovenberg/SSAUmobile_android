<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vldmr.ssaumobile">

    <permission
        android:name="vldmr.ssaumobile.permission.MAPS_RECEIVE"
        android:protectionLevel="signature"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_GPS" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="vldmr.ssaumobil.permission.MAPS_RECEIVE" />




    <application

        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyBCQ2hemtsaT6OAC8L87VTNvtLfh0J_z7M" />
        <uses-library android:name="com.google.android.maps" />


        <activity
            android:name=".activities.News"
            android:label="News">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.Media"
            android:label="Events"></activity>

        <activity
            android:name=".activities.Item"
            android:label="Events"></activity>

        <activity
            android:name=".activities.Tag"
            android:label="Events"></activity>
        <activity
            android:name=".activities.Search"
            android:label="Events"
            android:windowSoftInputMode="stateHidden"></activity>
        <activity
            android:name=".activities.Like"
            android:label="Events"></activity>
        <activity
            android:name=".activities.University"
            android:label="Events"></activity>
        <activity
            android:name=".activities.Map"
            android:label="Map"></activity>

-->
        <activity
            android:name=".activities.Events"
            android:label="Events"></activity>
        <activity
            android:name=".activities.Catalog"
            android:label="Events"></activity>

        <activity
            android:name=".activities.ItemEvent"
            android:label="Events"></activity>
        <activity
            android:name=".activities.About"
            android:label="Events"></activity>
    </application>

</manifest>
